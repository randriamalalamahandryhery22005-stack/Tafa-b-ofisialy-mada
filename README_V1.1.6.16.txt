TAFA V1.1.6.16 — Security & RLS Audit

This package starts from V1.1.6.15 and adds a diagnostic-only Supabase audit script.

File:
SECURITY_RLS_AUDIT_V1.1.6.16.sql

The SQL only READS metadata. It does not alter tables, policies, grants, functions, or data.
Run it in Supabase SQL Editor and review all result sets before applying any security fixes.
