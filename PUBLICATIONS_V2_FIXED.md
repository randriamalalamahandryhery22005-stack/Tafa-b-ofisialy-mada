# Publications V2 Fixed
Fixes the publication INSERT path, supports owner_id/user_id schema compatibility,
preserves the text immediately after publishing, and improves RLS/schema errors.
Auth and the existing Supabase architecture are unchanged.
